{
  "name": "kraken-pro-futures-signal-desk",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node dev-server.mjs",
    "test": "node --test",
    "test:integration": "node --test tests/setup-repository.integration.test.mjs",
    "check": "node scripts/check-syntax.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}

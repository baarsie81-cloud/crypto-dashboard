import "./order-book.js";
import { MARKET_LIMITS, SIGNAL_LIMITS, TIMEFRAMES, TRADE_DEFAULTS } from "./constants.js";
import { KrakenClient } from "./kraken.js";
import { analyzeMarket } from "./signals.js";
import { blendSignalWithContext, emptyContext, normalizeContext } from "./context-engine.js";
import { renderDecisionCards } from "./decision-ui.js";
import { CORE_SYMBOLS, passes85TradeGate, selectTradeUniverse, TRADE_UNIVERSE_LIMIT } from "./trade-universe.js";

const client = new KrakenClient();
const state = { markets:new Map(), tickers:new Map(), candles:{}, signals:new Map(), contexts:new Map(), universe:[] };
const assetName=(symbol)=>symbol==="PF_XBTUSD"?"BTC":symbol==="PF_ETHUSD"?"ETH":String(symbol).replace(/^PF_/,"").replace(/USD$/,"");
function mergeTicker(symbol,patch){state.tickers.set(symbol,{...(state.tickers.get(symbol)||{}),...patch,symbol});}
function hardenSettingsUi(){const leverage=document.getElementById("maxLeverage");if(leverage){[...leverage.options].forEach(o=>{if(Number(o.value)>SIGNAL_LIMITS.absoluteMaxLeverage)o.remove();});if(Number(leverage.value)>SIGNAL_LIMITS.absoluteMaxLeverage)leverage.value=String(SIGNAL_LIMITS.defaultMaxLeverage);}const risk=document.getElementById("riskPercentage");if(risk){risk.max=String(TRADE_DEFAULTS.maximumRiskPct);if(Number(risk.value)>TRADE_DEFAULTS.maximumRiskPct)risk.value=String(TRADE_DEFAULTS.riskPct);}}
async function loadContext(symbol){const asset=assetName(symbol);if(!CORE_SYMBOLS.includes(symbol)){state.contexts.set(symbol,emptyContext(asset));return;}try{const r=await fetch(`/api/context/${asset.toLowerCase()}`,{cache:"no-store",headers:{accept:"application/json"}});if(!r.ok)throw new Error();state.contexts.set(symbol,normalizeContext(await r.json(),asset));}catch{state.contexts.set(symbol,emptyContext(asset));}}
function evaluate(symbol){const market=state.markets.get(symbol),ticker=state.tickers.get(symbol)||{};if(!market)return;const technical=analyzeMarket({symbol,candlesByTimeframe:state.candles[symbol]||{},ticker,instrument:market,turnoverQuality:.9,dataAgeMs:Date.now()-(Number(ticker.receivedAt)||0),maxLeverage:SIGNAL_LIMITS.defaultMaxLeverage});state.signals.set(symbol,blendSignalWithContext(technical,state.contexts.get(symbol)||emptyContext(assetName(symbol))));}
function gateFor(symbol){return passes85TradeGate(state.signals.get(symbol),{symbol,btcSignal:state.signals.get("PF_XBTUSD")});}
function ensureDesk(){let desk=document.getElementById("decisionDesk");if(desk)return desk;const main=document.querySelector(".dashboard-grid");desk=document.createElement("section");desk.id="decisionDesk";desk.className="decision-desk panel primary-decision-desk";desk.innerHTML=`<div class="panel-heading decision-heading"><div><h2>85+ Trade Decision Desk</h2><span>Dynamische top-${TRADE_UNIVERSE_LIMIT} · BTC/ETH altijd · strengere altcoin-gates</span></div><small>Alleen A/A+ met bevestigde setup kan tradewaardig worden.</small></div><div id="decisionSummary" class="decision-summary"></div><div id="decisionCards" class="decision-cards"><p class="empty-copy">Universum wordt geladen…</p></div>`;main?.prepend(desk);return desk;}
function render(){ensureDesk();hardenSettingsUi();const cards=document.getElementById("decisionCards"),summary=document.getElementById("decisionSummary");if(!cards)return;const contexts=state.universe.map(m=>{const signal=state.signals.get(m.symbol),ticker=state.tickers.get(m.symbol);return signal?{market:m,ticker,signal}:null;}).filter(Boolean).sort((a,b)=>(Number(b.signal.score)||0)-(Number(a.signal.score)||0));const eligible=contexts.filter(c=>gateFor(c.market.symbol).eligible);const near=contexts.filter(c=>!gateFor(c.market.symbol).eligible&&(Number(c.signal.score)||0)>=75);if(summary){summary.innerHTML=`<div><span>Universum</span><strong>${state.universe.length}/${TRADE_UNIVERSE_LIMIT}</strong></div><div><span>85+ tradewaardig</span><strong class="${eligible.length?"positive":"neutral"}">${eligible.length}</strong></div><div><span>75–84 watch</span><strong>${near.length}</strong></div><div><span>BTC-regime</span><strong>${state.signals.get("PF_XBTUSD")?.marketRegime||"—"}</strong></div>`;}if(!contexts.length){cards.innerHTML=`<p class="empty-copy">Marktdata wordt geladen…</p>`;return;}cards.innerHTML=contexts.map(c=>{const gate=gateFor(c.market.symbol);const wrapper=document.createElement("div");renderDecisionCards(wrapper,[c]);const card=wrapper.innerHTML;const reason=gate.eligible?`<div class="trade-ready-banner">85+ TRADE READY · ${c.signal.status} · ${c.signal.tradeQuality}</div>`:`<div class="trade-gate-note">${(Number(c.signal.score)||0)>=75?`Nog niet tradewaardig: ${gate.reasons.slice(0,2).join(" · ")}`:"Onder 75 — alleen context"}</div>`;return `<div class="universe-decision ${gate.eligible?"is-ready":""}">${reason}${card}</div>`;}).join("");}
async function load(){ensureDesk();hardenSettingsUi();try{const [markets,tickers]=await Promise.all([client.getInstruments(),client.getTickers()]);markets.forEach(m=>state.markets.set(m.symbol,m));tickers.forEach(t=>mergeTicker(t.symbol,t));state.universe=selectTradeUniverse(markets,state.tickers);await Promise.all(state.universe.map(m=>loadContext(m.symbol)));await Promise.all(state.universe.flatMap(m=>Object.keys(TIMEFRAMES).map(async interval=>{state.candles[m.symbol]||={};state.candles[m.symbol][interval]=await client.getCandles(m.symbol,interval,MARKET_LIMITS.chartHistory);})));state.universe.forEach(m=>evaluate(m.symbol));render();const symbols=state.universe.map(m=>m.symbol);client.connectPublic(symbols,{getBookOptions(symbol){const m=state.markets.get(symbol);return{contractSize:m?.contractSize||1,targetNotionalUSD:1000};},onTicker(symbol,t){if(!state.markets.has(symbol))return;mergeTicker(symbol,t);evaluate(symbol);render();},onBookMetrics(symbol,m){mergeTicker(symbol,m);evaluate(symbol);render();},onBookInvalid(symbol){mergeTicker(symbol,{bookValidated:false});evaluate(symbol);render();}});setInterval(async()=>{await Promise.all(CORE_SYMBOLS.map(loadContext));state.universe.forEach(m=>evaluate(m.symbol));render();},5*60*1000);}catch(error){const cards=document.getElementById("decisionCards");if(cards)cards.innerHTML=`<p class="empty-copy">Decision Desk kon niet volledig laden. Probeer de marktdata te vernieuwen.</p>`;console.warn("85+ Decision Desk unavailable",error);}}
window.addEventListener("DOMContentLoaded",load);
